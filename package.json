{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "postbuild": "test -f public/build/manifest.json && echo 'Manifest exists' || (test -f public/build/.vite/manifest.json && cp public/build/.vite/manifest.json public/build/manifest.json && echo 'Copied manifest from .vite' || (echo 'ERROR: No manifest found!' && exit 1))"
    },
    "devDependencies": {
        "@tailwindcss/forms": "^0.5.7",
        "alpinejs": "^3.13.3",
        "autoprefixer": "^10.4.17",
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0",
        "postcss": "^8.4.33",
        "tailwindcss": "^3.4.1",
        "vite": "^5.1.0"
    }
}

